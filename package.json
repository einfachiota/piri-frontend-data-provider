{
  "name": "piri-frontend-data-provider",
  "version": "0.0.1",
  "description": "fetches data from piri, calculates score and returns all nodes",
  "main": "index.js",
  "scripts": {
    "start": "micro",
    "dev": "micro-dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "micro": "^9.3.3",
    "node-fetch": "latest"
  },
  "devDependencies": {
    "micro-dev": "^3.0.0"
  }
}
